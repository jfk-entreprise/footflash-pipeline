# === SECRETS — NE JAMAIS COMMITTER (contiennent des cles/token en clair) ===
**/config.md
*.env
.env
.env.*
cookies.txt
**/cookies.txt

# === Etats internes du pipeline ===
# NB : les fichiers d'etat anti-doublon (.processed/.downloaded/.analyzed/
# .scripted/.voiced/.rendered/validation_state) sont VOLONTAIREMENT SUIVIS par
# git afin de PERSISTER entre les runs GitHub Actions (runners ephemeres).
# Ne pas les re-ignorer, sinon le pipeline re-traiterait tout a chaque run.
**/__pycache__/
*.pyc

# === Divers ===
.DS_Store
Thumbs.db
